# @starknet-react/core@5 declares peer starknet@^8; we use starknet@9 for the API (Account, RpcProvider).
# This allows npm install to succeed. Remove if you switch to starknet@8.
legacy-peer-deps=true
